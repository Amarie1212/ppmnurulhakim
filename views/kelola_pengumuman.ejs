<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/styles.css" />
  <link rel="stylesheet" href="/duty.css" />
  <link rel="stylesheet" href="/toast.css" />
  
  <%- include('partials/notify') %>
  <style>
    .main-content {
      padding-top: 100px;
      padding-bottom: 40px;
    }

    .page-header {
      margin-bottom: 32px;
    }
    .page-header h1 {
      font-size: 1.5rem;
      font-weight: 800;
      margin: 0 0 8px 0;
      color: var(--text);
    }
    .page-header p {
      color: var(--muted);
      margin: 0;
      font-size: 0.95rem;
    }

    .form-section {
      background: var(--bg-2);
      border: 1px solid var(--border);
      border-radius: 2px;
      padding: 24px;
      margin-bottom: 20px;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 20px;
    }
    .section-icon {
      width: 44px;
      height: 44px;
      background: rgba(34, 197, 94, 0.1);
      border-radius: 2px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--accent);
    }
    .section-icon svg { width: 22px; height: 22px; }
    .section-title {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--text);
      margin: 0;
    }
    .section-subtitle {
      font-size: 0.85rem;
      color: var(--muted);
      margin: 4px 0 0 0;
    }

    .form-group {
      margin-bottom: 20px;
    }
    .form-group:last-child {
      margin-bottom: 0;
    }
    .form-label {
      display: block;
      font-size: 0.9rem;
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--text);
    }
    .form-input {
      width: 100%;
      padding: 12px 16px;
      border-radius: 8px;
      border: 1px solid var(--border);
      background: var(--bg);
      color: var(--text);
      font-size: 1rem;
      font-family: inherit;
      transition: all 0.2s;
    }
    .form-input:focus {
      outline: none;
      border-color: var(--accent);
    }
    .form-textarea {
      min-height: 120px;
      resize: vertical;
      line-height: 1.6;
    }
    .form-hint {
      font-size: 0.8rem;
      color: var(--muted);
      margin-top: 8px;
    }

    /* Contact Row */
    .contact-list {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .contact-row {
      display: grid;
      grid-template-columns: 120px 1fr 1fr 44px;
      gap: 10px;
      align-items: center;
      padding: 12px;
      background: var(--bg);
      border: 1px solid var(--border);
      border-radius: 8px;
    }
    .contact-row select,
    .contact-row input {
      padding: 10px 12px;
      border-radius: 8px;
      border: 1px solid var(--border);
      background: var(--bg-2);
      color: var(--text);
      font-size: 0.9rem;
    }
    .btn-remove-contact {
      width: 44px;
      height: 44px;
      border-radius: 8px;
      border: none;
      background: #ef4444;
      color: #fff;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.2s;
    }
    .btn-remove-contact:hover {
      background: #dc2626;
      color: #fff;
    }
    .btn-add-contact {
      padding: 14px;
      border: 2px dashed var(--border);
      border-radius: 8px;
      background: transparent;
      color: var(--accent);
      font-weight: 600;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      margin-top: 12px;
    }
    .btn-add-contact:hover {
      border-color: var(--accent);
      background: rgba(34, 197, 94, 0.05);
    }

    /* Action Buttons */
    .form-actions {
      display: flex;
      gap: 12px;
      margin-top: 24px;
      padding-top: 24px;
      border-top: 1px solid var(--border);
    }
    .form-actions .btn {
      flex: 1;
      justify-content: center;
      border-radius: 50px;
    }
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 24px;
      border-radius: 2px;
      font-weight: 600;
      font-size: 0.95rem;
      cursor: pointer;
      transition: all 0.2s;
      border: none;
      text-decoration: none;
    }
    .btn-secondary {
      background: var(--bg);
      border: 1px solid var(--border);
      color: var(--text);
    }
    .btn-secondary:hover {
      background: var(--bg-2);
    }
    .btn-primary {
      background: var(--accent);
      color: #000;
    }
    .btn-primary:hover {
      filter: brightness(1.1);
      box-shadow: 0 4px 12px rgba(34, 197, 94, 0.3);
    }

    @media (max-width: 768px) {
      .contact-row {
        grid-template-columns: 1fr;
        gap: 8px;
      }
      .btn-remove-contact {
        width: 100%;
      }
      .form-actions {
        flex-direction: column-reverse;
      }
      .btn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>

<header class="navbar">
  <div class="container nav-inner">
    <div class="brand-wrap">
      <a href="/pengurus/home" class="btn-back-inline" title="Kembali ke Dashboard">
        <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
      </a>
      <div class="brand">Kelola Pengumuman</div>
    </div>
    <div class="right-actions">
      <button id="navToggle" class="nav-toggle" style="display:flex !important;" aria-label="Menu">
        <div class="bar"></div><div class="bar"></div><div class="bar"></div>
      </button>
    </div>
  </div>
</header>

<%- include('partials/pengurus_sidebar', { currentPage: 'kelola-pengumuman', user: user, stat: null }) %>

<main class="section main-content">
  <div class="container" style="max-width: 800px;">

    <form id="pengumumanForm" action="/pengurus/kelola-pengumuman" method="POST">
      
      <!-- Section: Jadwal Kedatangan -->
      <div class="form-section">
        <div class="section-header">
          <div class="section-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
          </div>
          <div>
            <h3 class="section-title">Jadwal Kedatangan</h3>
            <p class="section-subtitle">Tanggal open house / kedatangan santri</p>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Tanggal Kedatangan</label>
          <input type="date" class="form-input" name="tanggal_kedatangan" style="max-width: 300px;" value="<%= info.tanggal_kedatangan ? new Date(info.tanggal_kedatangan).toISOString().split('T')[0] : '' %>">
          <p class="form-hint">Akan ditampilkan dengan countdown di halaman Info Santri</p>
        </div>
      </div>

      <!-- Section: Isi Pengumuman -->
      <div class="form-section">
        <div class="section-header">
          <div class="section-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
          </div>
          <div>
            <h3 class="section-title">Pengumuman</h3>
            <p class="section-subtitle">Pesan penting untuk para santri</p>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Isi Pengumuman</label>
          <textarea class="form-input form-textarea" name="pengumuman" placeholder="Tulis pengumuman untuk santri..."><%= info.pengumuman || '' %></textarea>
        </div>
      </div>

      <!-- Section: Peraturan Santri -->
      <div class="form-section">
        <div class="section-header">
          <div class="section-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
          </div>
          <div>
            <h3 class="section-title">Peraturan Santri</h3>
            <p class="section-subtitle">Daftar peraturan yang wajib dipatuhi</p>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Daftar Peraturan (satu per baris)</label>
          <% 
            const defaultPeraturan = [
              'Wajib mengikuti seluruh kegiatan pondok (sholat berjamaah, kajian, piket).',
              'Dilarang membawa kendaraan bermotor ke area pondok.',
              'Wajib menjaga kebersihan kamar dan lingkungan asrama.',
              'Berpakaian sopan dan rapi sesuai syariat Islam.'
            ];
            const peraturanList = (Array.isArray(info.peraturan) && info.peraturan.length > 0) ? info.peraturan : defaultPeraturan;
          %>
          <textarea class="form-input form-textarea" name="peraturan" rows="8" placeholder="Tuliskan peraturan..."><%= peraturanList.join('\n') %></textarea>
          <p class="form-hint">Gunakan Enter untuk memisahkan setiap poin peraturan</p>
        </div>
      </div>

      <!-- Section: Kontak Panitia -->
      <div class="form-section">
        <div class="section-header">
          <div class="section-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
          </div>
          <div>
            <h3 class="section-title">Kontak Panitia</h3>
            <p class="section-subtitle">Kontak yang bisa dihubungi santri</p>
          </div>
        </div>
        
        <div id="kontak-list" class="contact-list">
          <% 
            var kontakPanitiaData = (info && info.kontak_panitia) ? (typeof info.kontak_panitia === 'string' ? JSON.parse(info.kontak_panitia) : info.kontak_panitia) : [];
          %>
          <% kontakPanitiaData.forEach((k, i) => { %>
          <div class="contact-row">
            <select name="kontak_tipe[]">
              <option value="whatsapp" <%= k.tipe === 'whatsapp' ? 'selected' : '' %>>WhatsApp</option>
              <option value="email" <%= k.tipe === 'email' ? 'selected' : '' %>>Email</option>
              <option value="instagram" <%= k.tipe === 'instagram' ? 'selected' : '' %>>Instagram</option>
              <option value="phone" <%= k.tipe === 'phone' ? 'selected' : '' %>>Telepon</option>
            </select>
            <input type="text" name="kontak_label[]" placeholder="Nama/Label" value="<%= k.label || '' %>">
            <input type="text" name="kontak_value[]" placeholder="Nomor/ID/Username" value="<%= k.value || '' %>">
            <button type="button" class="btn-remove-contact" onclick="this.closest('.contact-row').remove()">
              <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
            </button>
          </div>
          <% }); %>
        </div>
        
        <button type="button" class="btn-add-contact" onclick="addKontakRow()">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          Tambah Kontak
        </button>
      </div>

      <!-- Action Buttons -->
      <div class="form-actions">
        <a href="/pengurus/home" class="btn btn-secondary">Batal</a>
        <button type="submit" class="btn btn-primary">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="18" height="18"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
          Simpan Perubahan
        </button>
      </div>

    </form>
  </div>
</main>

<button id="themeFab" class="theme-fab"></button>

<%- include('partials/pengurus_nav_script') %>

<script src="/toast.js"></script>

<script>
  (function() {
    window.addKontakRow = function() {
      const list = document.getElementById('kontak-list');
      const div = document.createElement('div');
      div.className = 'contact-row';
      div.innerHTML = `
        <select name="kontak_tipe[]">
          <option value="whatsapp">WhatsApp</option>
          <option value="email">Email</option>
          <option value="instagram">Instagram</option>
          <option value="phone">Telepon</option>
        </select>
        <input type="text" name="kontak_label[]" placeholder="Nama/Label">
        <input type="text" name="kontak_value[]" placeholder="Nomor/ID/Username">
        <button type="button" class="btn-remove-contact" onclick="this.closest('.contact-row').remove()">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      `;
      list.appendChild(div);
    };
  })();
</script>

<% if (typeof success !== 'undefined' && success) { %>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    showToast('success', 'Berhasil!', 'Pengumuman berhasil disimpan.');
    if (window.history.replaceState) {
      const url = new URL(window.location);
      url.searchParams.delete('success');
      window.history.replaceState({}, document.title, url);
    }
  });
</script>
<% } %>

</body>
</html>
